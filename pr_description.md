🎨 Palette: [UX improvement]

💡 What: Applied a new visual blur style (glassmorphism) matching `RockMusicExperience` uniformly to `RockMusicRoot` and `AppearanceScreen`.
🎯 Why: To create a consistent glassmorphism effect with deep translucent frames (alpha=0.2f), sheen gradients, 14dp blurs, gradient stroke boundaries, and matching shadows as per user request and reference images.
📸 Before/After: Visual update to `GlassFrame` surfaces across navigation layouts and `AppearancePreview`.
♿ Accessibility: N/A
